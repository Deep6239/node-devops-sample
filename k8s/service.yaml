apiVersion: v1
kind: Service

metadata:
  name: mern-devops-service

spec:
  type: LoadBalancer

  selector:
    app: mern-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      